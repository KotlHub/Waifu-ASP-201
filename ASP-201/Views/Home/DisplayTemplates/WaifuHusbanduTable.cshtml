@model List<ASP_201.Data.Entity.Waifu>

<div class="waifu-container">
    <div class="waifu-center">
        @for (int i = 0; i < Model.Count; ++i) {
            @Html.DisplayFor(m => m[i])
        }
    </div>
</div>